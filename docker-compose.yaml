services:
  backend:
    build:
      context: ./backend
    container_name: taskflow-backend
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    container_name: taskflow-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend